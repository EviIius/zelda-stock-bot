# Required for rendered retailer checks.
# Playwright 1.56+ dropped the Chromium build used by macOS 13. Windows and
# newer non-Mac hosts can continue following the current release.
playwright>=1.44,<1.56; sys_platform == "darwin"
playwright>=1.44; sys_platform != "darwin"
